name: jimple-threads-ci

# Focused build+test loop for the Jimple thread-support work (public fork: free CI).
# Builds ESBMC with build.sh, then sanity-checks: --version, a known jimple fixture
# (kt-array-false -> FAILED), the C pthread race (-> FAILED, proves the concurrency
# engine), and our jimple thread test once it exists.
on:
  workflow_dispatch:
  push:
    branches: [jimple-threads]
    paths: ['src/jimple-frontend/**', 'src/goto-symex/**', 'jimple-threads-spike/**', '.github/workflows/jimple-threads-ci.yml']

jobs:
  build-and-test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      # Cache the built binary keyed on the SOURCES. A fixture-only or
      # workflow-only change leaves src/ unchanged -> exact key hit -> restore
      # release/ and skip the entire build (deps+compile+link) and all the
      # build-only caches below. Only a real source change misses and rebuilds.
      # This is what makes fixture iterations ~1 min instead of ~6.
      - name: Cache built esbmc binary
        id: bincache
        uses: actions/cache@v4
        with:
          path: release
          key: esbmc-bin-${{ runner.os }}-${{ hashFiles('src/**', 'scripts/build.sh', 'scripts/cmake/**', 'CMakeLists.txt') }}

      - name: Cache GMP source tarball
        if: steps.bincache.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: |
            .deps
          key: esbmc-deps-${{ runner.os }}-v1

      # The heavy "deps" cost is FetchContent downloading + xz-extracting ~1.5 GB
      # of prebuilt LLVM (+Z3/CVC5) into the ephemeral build/_deps every run.
      # Redirect FetchContent's base dir to this stable, cached location so the
      # download+extract happens once; later runs reuse it (URL-hash stamps make
      # FetchContent skip the work). Keyed on the dependency URLs.
      - name: Cache prebuilt LLVM/solvers (FetchContent)
        if: steps.bincache.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: ~/esbmc-fc
          key: esbmc-fc-${{ runner.os }}-${{ hashFiles('scripts/cmake/Options.cmake', 'scripts/cmake/ExternalDependencies.cmake') }}

      # ccache caches compiled object files across runs. Since iterations touch
      # only a few jimple-frontend files, warm builds recompile just those +
      # relink, dropping the ~20-min cold build to a few minutes.
      - name: ccache
        if: steps.bincache.outputs.cache-hit != 'true'
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: esbmc-jimple-${{ runner.os }}
          max-size: 2G

      # Phase 1: configure deps. EXTRA_CMAKE_ARGS points FetchContent at the
      # cached dir above (build.sh forwards it to cmake). Skipped on a binary hit.
      - name: Configure (deps)
        if: steps.bincache.outputs.cache-hit != 'true'
        run: |
          sudo apt-get install -y -q lld >/dev/null 2>&1 || true
          export EXTRA_CMAKE_ARGS="-DFETCHCONTENT_BASE_DIR=$HOME/esbmc-fc"
          ./scripts/build.sh deps

      # Phase 2: reconfigure for speed — ccache, the lld linker (the static link
      # of LLVM+solvers dominates a source-change build), and drop frontends/
      # solvers this spike doesn't need (we only use Jimple + Z3).
      - name: Trim build (ccache + lld + minimal frontends/solvers)
        if: steps.bincache.outputs.cache-hit != 'true'
        run: |
          cmake build \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
            -DCMAKE_EXE_LINKER_FLAGS=-fuse-ld=lld \
            -DENABLE_PYTHON_FRONTEND=Off \
            -DENABLE_SOLIDITY_FRONTEND=Off \
            -DENABLE_BOOLECTOR=Off \
            -DENABLE_BITWUZLA=Off \
            -DENABLE_CVC5=Off \
            -DENABLE_CSMITH=Off \
            -DBUILD_TESTING=Off \
            -DENABLE_REGRESSION=Off

      # Phase 3: compile + install (cmake --build / --install, via build.sh).
      - name: Build + install ESBMC
        if: steps.bincache.outputs.cache-hit != 'true'
        run: ./scripts/build.sh build install

      - name: Locate esbmc binary
        id: bin
        run: |
          ESBMC=$(find "$PWD/release" -name esbmc -type f | head -1)
          echo "esbmc=$ESBMC" >> "$GITHUB_OUTPUT"
          "$ESBMC" --version

      # Diagnostic: dump the GOTO for the long-mul fixture so we can see the
      # exact types/widths the multiply and its operands carry.
      - name: Diagnose long-mul GOTO + types
        run: |
          ESBMC="${{ steps.bin.outputs.esbmc }}"
          echo "===== goto-functions-only ====="
          "$ESBMC" regression/jimple/long-mul-true/main.jimple --force-malloc-success --goto-functions-only 2>&1 | sed -n '1,120p' || true
          echo "===== verbose verify ====="
          "$ESBMC" regression/jimple/long-mul-true/main.jimple --force-malloc-success 2>&1 | tail -25 || true

      - name: Upload built esbmc binary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: esbmc-linux-jimple-primitives
          path: release/bin/esbmc
          if-no-files-found: warn

      - name: Sanity — jimple fixture (expect VERIFICATION FAILED)
        run: |
          "${{ steps.bin.outputs.esbmc }}" regression/jimple/kt-array-false/main.jimple --force-malloc-success | tail -3

      # Primitive-width gate: 65536L*65536L = 2^32 is nonzero in 64-bit but
      # truncates to 0 under the old long->int32 bug; the fixture divides by it.
      # Correct 64-bit long => no division by zero => VERIFICATION SUCCESSFUL.
      - name: Primitive types — long not truncated (expect SUCCESSFUL)
        run: |
          ESBMC="${{ steps.bin.outputs.esbmc }}"
          "$ESBMC" regression/jimple/long-mul-true/main.jimple --force-malloc-success > lm.log 2>&1 || true
          cat lm.log | tail -8
          grep -q "VERIFICATION SUCCESSFUL" lm.log || { echo "FAIL: long truncation not fixed (expected SUCCESSFUL)"; exit 1; }
          echo "PASS: long is 64-bit"

      # Native assert/assume gate. Each fixture exercises the wired jimple
      # Assertion (-> GOTO ASSERT, code_assertt) and Assume (-> GOTO ASSUME,
      # code_assumet) statements over nondet inputs, on the rebuilt binary. The
      # two -> FAILED cases prove real all-inputs checking; assume-noverprune
      # proves an assume narrows without over-pruning; contradiction proves a
      # genuinely vacuous proof. The verdict in test.desc must match exactly.
      - name: Native assert/assume gate
        run: |
          ESBMC="${{ steps.bin.outputs.esbmc }}"
          fail=0
          for t in assert-mul0-true:SUCCESSFUL \
                   assert-eq5-false:FAILED \
                   assume-narrow-true:SUCCESSFUL \
                   assume-noverprune-false:FAILED \
                   bounded-anyint-false:FAILED \
                   assume-contradiction-vacuous:SUCCESSFUL; do
            name="${t%%:*}"; expect="${t##*:}"
            jf="regression/jimple/$name/main.jimple"
            echo "==================== $name (expect $expect) ===================="
            set +e
            "$ESBMC" "$jf" --force-malloc-success --z3 > aa.log 2>&1
            rc=$?
            set -e
            tail -6 aa.log
            echo "--- $name exit code: $rc ---"
            grep -q "VERIFICATION $expect" aa.log || { echo "FAIL[$name]: expected VERIFICATION $expect"; fail=1; continue; }
            echo "PASS[$name]"
          done
          exit $fail

      - name: Sanity — C pthread race (expect VERIFICATION FAILED, engine concurrency)
        run: |
          [ -f jimple-threads-spike/race.c ] || { echo "no race.c on this branch"; exit 0; }
          "${{ steps.bin.outputs.esbmc }}" jimple-threads-spike/race.c --context-bound 2 | tail -3

      # Regression gates. Each fixture must reach VERIFICATION FAILED with the
      # division-by-zero property; a crash, a SUCCESSFUL verdict, or a different
      # property fails the job. Covers: single-class thread race (thread), the
      # multi-class linker (multiclass: A.main calls B.answer), a real-shape
      # 2-class thread program (mcthread: cross-class static field + instance
      # run() spawned from another class), and javathread — the SAME program
      # emitted by our Soot-based converter from real javac bytecode
      # (Main/Worker.java), proving the producer end-to-end.
      - name: Jimple thread + multi-class gates (expect FAILED, division by zero)
        run: |
          ESBMC="${{ steps.bin.outputs.esbmc }}"
          fail=0
          for f in thread multiclass mcthread javathread runnablethread kotlinlambda; do
            jf="jimple-threads-spike/$f.jimple"
            [ -f "$jf" ] || { echo "skip: $jf absent"; continue; }
            echo "==================== $f ===================="
            set +e
            stdbuf -oL -eL "$ESBMC" "$jf" --force-malloc-success --context-bound 2 > tj.log 2>&1
            rc=$?
            set -e
            cat tj.log
            echo "--- $f exit code: $rc ---"
            if [ "$rc" -ge 128 ]; then
              echo "FAIL[$f]: esbmc crashed (signal). gdb backtrace:"
              sudo apt-get install -y -q gdb >/dev/null 2>&1 || true
              gdb -q -batch -ex 'run' -ex 'bt' --args \
                "$ESBMC" "$jf" --force-malloc-success --context-bound 2 2>&1 | tail -40 || true
              fail=1; continue
            fi
            grep -q "VERIFICATION FAILED" tj.log || { echo "FAIL[$f]: expected VERIFICATION FAILED"; fail=1; continue; }
            grep -qi "division by zero" tj.log || { echo "FAIL[$f]: expected division-by-zero property"; fail=1; continue; }
            echo "PASS[$f]"
          done
          exit $fail

      # Gate: concurrent Kotlin coroutines. Real kotlinx program (runBlocking {
      # launch(Dispatchers.Default){counter++} x2; read counter }) -> the
      # lost-update interleaving must trip the division by zero. runBlocking runs
      # inline, launch -> __ESBMC_spawn_thread. Pointer/alignment checks are off:
      # the continuation/scope objects are modelled as opaque, so their derefs are
      # scaffolding artifacts, not the property under test.
      - name: Coroutine gate (expect FAILED, division by zero)
        run: |
          jf="jimple-threads-spike/coroutine.jimple"
          [ -f "$jf" ] || { echo "no coroutine.jimple"; exit 0; }
          set +e
          "${{ steps.bin.outputs.esbmc }}" "$jf" --force-malloc-success \
            --no-pointer-check --no-align-check --context-bound 4 > co.log 2>&1
          rc=$?
          set -e
          cat co.log
          echo "--- coroutine exit code: $rc ---"
          grep -q "VERIFICATION FAILED" co.log || { echo "FAIL: expected VERIFICATION FAILED"; exit 1; }
          grep -qi "division by zero" co.log || { echo "FAIL: expected division-by-zero property"; exit 1; }
          echo "PASS[coroutine]: concurrent launch{} lost-update race detected"
